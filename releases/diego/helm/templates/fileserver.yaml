{{- if .Values.fileserver.enabled }}
---
kind: Service
apiVersion: v1
metadata:
  name: file-server
  labels:
    app.kubernetes.io/name: file-server
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: file-server
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-server
  labels:
    app.kubernetes.io/name: file-server
  annotations:
    checksum/config: {{ include (print $.Template.BasePath "/fileserver-configmap.yaml") . | sha256sum }}
spec:
  # This is a singleton.
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: file-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: file-server
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/fileserver-configmap.yaml") . | sha256sum }}
    spec:
      containers:
        - name: nginx
          image: {{ .Values.fileserver.image.repository }}:{{ .Values.fileserver.image.tag }}
          imagePullPolicy: {{ .Values.fileserver.image.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
          {{- if .Values.fileserver.resources }}
          resources:
            {{- toYaml .Values.fileserver.resources | nindent 12 }}
          {{- end }}
      {{- if .Values.fileserver.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.fileserver.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.fileserver.tolerations }}
      tolerations:
        {{- toYaml .Values.fileserver.tolerations | nindent 8 }}
      {{- end }}
      volumes:
        - name: nginx-config
          configMap:
            name: fileserver-configmap
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: file-server
spec:
  parentRefs:
    - name: istio-gateway
  hostnames:
    - "{{ .Values.fileserver.hostname }}"
  rules:
    - backendRefs:
        - name: file-server
          port: 8080
{{- end }}